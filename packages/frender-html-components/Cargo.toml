[package]
name = "frender-html-components"
version = "0.1.0"
edition = "2021"

[dependencies]
frender-html = { version = "0.1.0", path = "../frender-html", default-features = false, features = [
    "components",
] }

[features]
csr = ["frender-html/csr"]
ssr = ["frender-html/ssr"]
