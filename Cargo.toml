[workspace]

members = [
    "frender",
    # TODO:
    "crates/*",
    "examples/*",
]

exclude = ["examples/tic-tac-toe"]

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s"
