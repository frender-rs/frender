#[allow(unused_imports)]
use super::*;

pub use frender_html::html::components::{Element, HtmlElement};
crate::imports::def_intrinsic_component_simple!(
    type Props = HtmlElement;
    type Element = HtmlElement;
    pub struct abbr;
    pub struct address;
    pub struct article;
    pub struct aside;
    pub struct b;
    pub struct bdi;
    pub struct bdo;
    pub struct cite;
    pub struct code;
    pub struct datalist;
    pub struct dd;
    pub struct dfn;
    pub struct div;
    pub struct dl;
    pub struct dt;
    pub struct em;
    pub struct figcaption;
    pub struct figure;
    pub struct footer;
    pub struct h1;
    pub struct h2;
    pub struct h3;
    pub struct h4;
    pub struct h5;
    pub struct h6;
    pub struct head;
    pub struct header;
    pub struct hgroup;
    pub struct hr;
    pub struct i;
    pub struct kbd;
    pub struct legend;
    pub struct main;
    pub struct mark;
    pub struct menu;
    pub struct nav;
    pub struct noscript;
    pub struct p;
    pub struct picture;
    pub struct pre;
    pub struct rp;
    pub struct rt;
    pub struct ruby;
    pub struct s;
    pub struct samp;
    pub struct section;
    pub struct small;
    pub struct span;
    pub struct strong;
    pub struct sub;
    pub struct summary;
    pub struct sup;
    pub struct template;
    pub struct title;
    pub struct u;
    pub struct var;
    pub struct wbr;
);

// pub use frender_html::html::components::{
//     abbr, address, article, aside, b, bdi, bdo, cite, code, datalist, dd, dfn, div, dl, dt, em,
//     figcaption, figure, footer, h1, h2, h3, h4, h5, h6, head, header, hgroup, hr, i, kbd, legend,
//     main, mark, menu, nav, noscript, p, picture, pre, rp, rt, ruby, s, samp, section, small, span,
//     strong, sub, summary, sup, template, title, u, var, wbr,
// };
