name: Release
on:
  push:
    branches:
      - v2

env:
  CARGO_TERM_COLOR: always

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "16"
      - name: Setup wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh
      # https://github.com/actions/cache/blob/master/examples.md#node---yarn
      - name: Get yarn cache directory path
        id: yarn-cache-dir-path
        run: echo "::set-output name=dir::./.yarn/cache"
      - uses: actions/cache@v2
        id: yarn-cache
        with:
          path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
          key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-yarn-
      - uses: actions/cache@v2
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
      - name: Install dependencies
        run: yarn install --immutable
      - name: Ensure linted
        run: yarn run ensure-linted
      - name: Test
        run: |
          set -e
          cargo check --verbose
          cargo test --verbose
          cargo check -p frender-hook-element
          cargo check -p frender-hook-element --features ssr
          cargo check -p frender-hook-element --features dom
          cargo check -p frender-hook-element --features ssr,dom
          wasm-pack test --node frender
      - name: Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          if [ -z "$GITHUB_TOKEN" ]; then
            echo 'Unexpected empty GITHUB_TOKEN' >&2
            exit 1
          fi
          if [ -z "$CARGO_REGISTRY_TOKEN" ]; then
            echo 'Please set secrets.CARGO_REGISTRY_TOKEN' >&2
            exit 1
          fi
          yarn run semantic-release
